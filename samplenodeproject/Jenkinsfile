stage('Deploy App') {
    steps {
        sh 'scp -r ./dist azureuser@51.142.192.35:/var/www/calculator'
        sh 'ssh azureuser@51.142.192.35 "sudo systemctl restart nginx"'
    }
}
stage('Verify') {
    steps {
        sh 'curl -f http://51.142.192.35:80 || exit 1'
    }
}