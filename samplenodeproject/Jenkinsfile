pipeline {
    agent any

    stages {
        stage('Initialize') {
            steps {
                echo 'Starting pipeline'
            }
        }

        stage('Run Stages in Subdirectory') {
            steps {
                script {
                    // Define the helper function
                    def runInMyDir = { stageName, body ->
                        stage(stageName) {
                            dir('samplenodeproject') {
                                body()
                            }
                        }
                    }

                    runInMyDir('Cleanup') {
                        sh 'chmod +x cleanup.sh && ./cleanup.sh'
                        // sh 'make build' or your real build command
                    }

                    runInMyDir('Build') {
                        sh 'docker build -t nodeapp .'
                        // sh 'make build' or your real build command
                    }

                    runInMyDir('Test') {
                        echo 'testing...'
                        sh 'docker inspect --type=image nodeapp:latest '

                    }

                    runInMyDir('Deploy') {
                        sh 'docker run -d --name nodeappcontainer -p 80:80  nodeapp:latest'
                    }
                }
            }
        }
    }



}
